import { createTheme } from "@mui/material";

export const principalTheme = createTheme({
  palette: {
    verificando_uy_light: {
      main: "#29B78B",
      light: "#94dbc5",
      dark: "#155c46",
      contrastText: "#000000",
    },
  },
});
